import axios from 'axios'

const Axios = axios.create({
    // baseURL: 'assets/Products/product_data.json',
    // timeout: 1000,
    // headers: { 'X-Custom-Header': 'foobar' },
});

export default Axios